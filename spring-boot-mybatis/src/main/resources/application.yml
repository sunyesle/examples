spring:
  application:
    name: spring-boot-mybatis
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:test'
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
decorator:
  datasource:
    p6spy:
      enable-logging: true
